// Stub file for missing checklist-rules.service module
export function buildBaseChecklistFromRules(
  userContext: any,
  ruleSet: any
): Array<{ documentType: string; category: string; required: boolean }> {
  return [];
}
